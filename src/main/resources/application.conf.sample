github {
    token=none
    id = "github-id"
}

akka {
    loglevel = DEBUG
    # log-config-on-start = on
}

mongo {
    test {
        host = "localhost"
        port = 27017
    }

    local {
        host = "localhost"
        port = 27017
    }

    production {
        host = "localhost"
        port = 27017
    }
}
