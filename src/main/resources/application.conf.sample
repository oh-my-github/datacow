akka {
    loglevel = DEBUG
    # log-config-on-start = on
}

mongo {
    test {
        host = "localhost"
        port = 29017
        db = "omg-test"
    }

    production {
        host = "localhost"
        port = 27017
        db = "omg-production"
    }
}
